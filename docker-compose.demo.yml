# ============================================
# DEPRECATO - Usa docker-compose.prod.yml
# ============================================
# Questo file è mantenuto per compatibilità.
# Per nuovi deploy, usa:
#   docker compose -f docker-compose.prod.yml up -d
#
# Vedi .env.production.example per la configurazione
# ============================================

# Importa la configurazione di produzione
include:
  - docker-compose.prod.yml
