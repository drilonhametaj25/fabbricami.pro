apiVersion: 1

providers:
  - name: 'EcommerceERP Dashboards'
    orgId: 1
    folder: 'ERP'
    folderUid: 'erp-folder'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
